# GitHub Pages 静态离线前端

这个目录是给 GitHub Pages 用的最小静态包。

使用方式：
1. 把这个目录内容发布到 GitHub Pages。
2. 手机第一次联网打开 Pages 地址，并添加到主屏幕。
3. 在服务器数据页下载 `基础数据 JSON`。
4. 在静态离线页点击 `导入基础数据 JSON`。
5. 离线录入后点击 `导出待同步 JSON`。
6. 回到服务器数据页，使用 `导入离线 JSON` 把记录写回数据库。
